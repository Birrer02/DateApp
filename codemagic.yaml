workflows:
  android-workflow:
    name: Android Build
    environment:
      flutter: stable
      android_signing:
        - keystore_reference
    scripts:
      - flutter clean
      - flutter pub get
      - flutter build apk --release
